# Database examples (SQL + vector DB)

1. Usage `asyncpg` lib
2. Advanced postgres working: datatypes, transactions, pools, extention `pgvector`
3. Qdrant DB example: docker-compose, vectorsearch retriver (langchain), fastapi backend